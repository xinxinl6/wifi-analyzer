# 2026-05-18 工作日志

## Ping 测试页面上移微调
- 用户反馈内容仍需继续上移
- `PingTestPage.ets`: 返回按钮行 padding bottom 从 `SPACE_SM(8vp)` → `4vp`
- `PingTestingView.ets`: Scroll 内容列 padding top 从 `SPACE_XS(4vp)` → `0vp`
- 合计再减少 8vp 顶部留白

## DNS 查询页面 UI 简化
- 用户反馈"太花哨"，统一为天蓝色系
- `DnsLookupPage.ets`:
  - DNS服务器标签：去掉各服务器自定义颜色，未选中统一灰色，选中统一 `COLOR_PRIMARY`
  - 记录类型说明：去掉彩虹色（A/AAAA/CNAME/MX/NS/TXT 各不同），统一 `COLOR_PRIMARY`
  - 搜索/查询按钮：`COLOR_SUCCESS` → `COLOR_PRIMARY`
  - 常用域名标签：绿色 → 天蓝色系背景+文字
  - FeatureCard：emoji 替换为图标资源，绿色背景 → 天蓝色
  - 结果页筛选标签/统计/重新查询按钮：绿色 → 天蓝色
  - 清理未使用的彩色导入（COLOR_SUCCESS/COLOR_PURPLE 等）
- `DnsHistoryModal.ets`:
  - 历史项图标/统计数字：`COLOR_SUCCESS` → `COLOR_PRIMARY`
  - 绿色背景 → 天蓝色背景

## 资源统一（P0+P1）— 大规模硬编码替换
- **触发**: 用户要求"资源统一"，替换所有硬编码字符串和颜色为 design-system Token / $r() 引用
- **4个并行Agent执行**:
  - Entry颜色: ~100+处替换，28个文件 (rgba→COLOR_OVERLAY/COLOR_GLASS_*等)
  - Tools颜色: ~206处rgba + 43处hex (tools模块颜色替换)
  - Entry字符串: 12处UI硬编码中文→$r()，3个新string.json条目
  - Tools字符串: 7处替换+5处fallback移除，7个新string.json条目
- **总计**: 67个文件变更，+4031/-11260行
- **新增string.json条目**: entry 3个 + tools 7个 = 10个
- **新增en_US/string.json**: 176行英文翻译

### 替换后剩余硬编码（P2级别，合理保留）
- **颜色 (~103处)**: 34处hex + 69处rgba
  - `#FFFFFF` 在彩色按钮/图标上的白色文字（8处，无对应Token）
  - 暗/亮色模式三元表达式中的rgba（如 `this.isDarkMode ? 'rgba(x,x,x,0.15)' : '#EEF2FF'`）
  - 渐变色数组、动态透明度表达式
  - Engine数据配置中的颜色（如DnsLookupEngine服务器颜色）
- **中文 (~9处UI层)**: 全部在Engine数据文件（PoeCalculator/PortScanEngine/WolEngine），非UI组件
- **建议P2补充**: 添加 `COLOR_TEXT_ON_COLOR` Token、浅色卡片背景Token（#EEF2FF/#ECFDF5/#F5F3FF等）
