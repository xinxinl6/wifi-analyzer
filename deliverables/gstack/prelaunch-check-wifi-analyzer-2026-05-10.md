# WiFi Analyzer 02 上线前全检报告

**日期**：2026-05-10
**场景**：上线前全检（代码审查 + 安全审计 + QA测试）
**参与成员**：代码审查专家 + 安全审计专家 + QA负责人

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟡 **有条件通过（Conditional Go）**
- 阻塞项数量：**3 个 P0 问题必须修复才能上线**
- 主要风险：明文密码泄露、不安全HTTP通信、WebView无安全验证
- 下一步：修复 P0 问题 → 重新审查 → 发布

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 有条件通过（需修复 3 个 P0 问题） |
| 严重度分布 | 🔴 5 / 🟠 0 / 🟡 6 / 🟢 4 |
| 关键行动项 | 8 条 |
| 建议负责人 | 技术负责人 + 安全负责人 |

---

## 1. 各成员核心结论

### 🕵️ 代码审查专家（Code Review）
- **核心判断**：有条件通过。代码架构清晰、模块化设计良好，但存在明文密码、权限过度申请、WebView 安全风险等问题。
- **关键建议**：
  1. 立即移除 `build-profile.json5` 中的明文密码
  2. 审查权限需求，确保最小权限原则
  3. 为 WebView 添加 URL 验证和安全配置

### 🛡️ 安全审计专家（OWASP + STRIDE）
- **核心判断**：中等风险。发现 10 个安全问题（3 高 / 4 中 / 3 低）。主要风险为不安全HTTP通信、日志信息泄露、过度权限。
- **关键建议**：
  1. 迁移所有 HTTP API 到 HTTPS（5个外部API）
  2. 清理/脱敏 hilog 中的敏感信息
  3. 移除 `SET_WIFI_INFO` 不必要权限

### ✅ QA 负责人（QA 测试）
- **核心判断**：Conditional Go。功能完整性良好，但存在配置安全、权限管理、构建产物等问题。
- **关键建议**：
  1. 修复构建配置中的明文密码（P0）
  2. 统一权限声明与运行时检查
  3. 设置 EntryAbility `exported: false`（如不需要外部调用）

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🔴 P0 | 配置安全 | `build-profile.json5:10-14` | 构建配置文件中 `keyPassword` 和 `storePassword` 以明文存储 | 使用环境变量或鸿蒙安全签名机制，避免明文密码 | 代码审查 + QA |
| 2 | 🔴 P0 | 网络安全 | `core/src/main/ets/network/NetworkDiagnostics.ets:185-191` | 使用 HTTP（非HTTPS）调用 5 个外部 API，存在中间人攻击风险 | 迁移到 HTTPS API，实现证书固定 | 安全审计 |
| 3 | 🔴 P0 | 信息安全 | 多个文件 `hilog.info()` | 日志中输出 SSID、BSSID、IP 地址等敏感信息，可能违反隐私法规 | 移除调试日志或对敏感信息脱敏 | 安全审计 |
| 4 | 🔴 P0 | Web安全 | `entry/src/main/ets/components/AuroraDashboard.ets:810` | AI 分析功能加载外部 URL，无 URL 验证和白名单，可能导致 XSS | 验证 URL 白名单，配置 WebView 安全设置 | 代码审查 |
| 5 | 🔴 P0 | 权限安全 | `entry/src/main/module.json5:22-24` | 声明 `SET_WIFI_INFO` 权限但可能不必要（只读分析工具），违反最小权限原则 | 移除不必要的 `SET_WIFI_INFO` 权限 | 安全审计 + QA |
| 6 | 🟡 P1 | 权限管理 | `core/src/main/ets/permission/PermissionManager.ets:12-15` | 只检查了 2 个权限（LOCATION、APPROXIMATELY_LOCATION），但 module.json5 声明了 6 个权限 | 检查所有声明的权限并在运行时验证 | 安全审计 + QA |
| 7 | 🟡 P1 | API 安全 | `core/src/main/ets/wifi/WifiService.ets:54-56` | 使用已废弃的 HarmonyOS WiFi API，未来系统更新可能导致功能失效或安全漏洞 | 迁移到最新的 HarmonyOS WiFi API | 安全审计 |
| 8 | 🟡 P1 | 数据安全 | `core/src/main/ets/wifi/WifiScanner.ets:350-356` | Fallback 数据包含疑似真实 SSID（"xinxin", "ZZB", "aWiFi"） | 使用通用占位符（如 "Sample_Network_1"） | 安全审计 |
| 9 | 🟡 P1 | 组件安全 | `entry/src/main/module.json5:59` | EntryAbility 设置 `exported: true`，可能允许其他应用启动此 Activity | 如不需要外部调用，设置 `exported: false` | QA + 安全审计 |
| 10 | 🟡 P1 | 网络安全 | `NetworkDiagnostics.ets:166-167` | 网关和 DNS 使用硬编码默认值（`192.168.1.1` 和 `223.5.5.5`），可能显示错误信息 | 设置为 `'--'` 或 `'未知'` 而不是假数据 | 代码审查 |
| 11 | 🟢 P2 | 代码质量 | `entry/src/main/ets/components/AuroraDashboard.ets:341-354` | `setInterval` 定时器在 `aboutToDisappear` 中清理，但可能清理不及时导致内存泄漏 | 使用 `AKit` 的定时器或增加保护逻辑 | 代码审查 |
| 12 | 🟢 P2 | 数据安全 | `core/src/main/ets/wifi/WifiScanner.ets:322-360` | 扫描失败时自动使用模拟数据，用户可能不知道数据不是真实的 | 在 UI 上明确标识当前显示的是模拟数据 | 代码审查 |
| 13 | 🟢 P2 | 安全增强 | 整体 | 未实现代码混淆和签名验证，攻击者可轻易逆向工程 | 在发布版本中启用代码混淆 | 安全审计 |
| 14 | 🟢 P2 | 安全增强 | 整体 | 缺乏安全审计日志，发生安全事件后难以追溯 | 实现安全日志记录关键操作 | 安全审计 |
| 15 | 🟢 P3 | 性能优化 | `WifiScanner.ets:74` | 建议考虑虚拟列表渲染以优化大数据集性能 | 实现虚拟列表渲染 | 代码审查 |

---

## ✅ 行动清单（按优先级排序）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | **移除 `build-profile.json5` 中的明文密码**，使用鸿蒙安全签名机制或环境变量 | 技术负责人 | P0 | 立即 |
| 2 | **迁移所有 HTTP API 调用到 HTTPS**（影响 5 个外部 API） | 技术负责人 | P0 | 1-2 天 |
| 3 | **清理/脱敏 hilog 中的敏感信息**（SSID、BSSID、IP 等） | 技术负责人 | P0 | 1-2 天 |
| 4 | **为 WebView 添加 URL 验证和白名单**，配置安全设置 | 技术负责人 | P0 | 2-3 天 |
| 5 | **移除不必要的 `SET_WIFI_INFO` 权限** | 技术负责人 | P0 | 1 天 |
| 6 | **统一权限检查**：检查所有声明的 6 个权限并在运行时验证 | 技术负责人 | P1 | 1 周 |
| 7 | **迁移到最新的 HarmonyOS WiFi API**，替换已废弃的 API | 技术负责人 | P1 | 2 周 |
| 8 | **设置 EntryAbility `exported: false`**（如不需要外部调用） | 技术负责人 | P1 | 1 天 |
| 9 | 替换 fallback 数据中的硬编码 SSID 为通用占位符 | 开发人员 | P2 | 1 周 |
| 10 | 启用代码混淆和签名验证 | 技术负责人 | P2 | 发布前 |

---

## ⚠️ 待完善 / 已知局限

1. **QA 测试受限**：由于无法在实际设备上运行，QA 测试主要基于代码审查和配置文件分析，未进行实际功能测试
2. **性能测试缺失**：未进行实际的性能测试（CPU、内存、电池消耗）
3. **兼容性测试缺失**：未在不同 HarmonyOS 版本和设备上测试
4. **安全测试工具限制**：未进行动态安全测试（如渗透测试、fuzzing）

---

## 📚 成员产出索引

- **代码审查专家** 原始产出：审查了 core/、entry/、design-system/ 目录，发现 3 个严重问题、3 个警告问题、若干信息性问题
- **安全审计专家** 原始产出：完成 OWASP Mobile Top 10 检查 + STRIDE 威胁建模，发现 10 个安全问题（3 高 / 4 中 / 3 低）
- **QA 负责人** 原始产出：基于代码审查和配置文件分析的 QA 评估，确认了 P0 问题并补充了权限管理和组件安全问题

---

> **本报告由 GStack 工程团队 AI 协作生成，关键决策请由工程负责人复核。**
>
> **Go/No-Go 决策**：🟡 **Conditional Go** — 必须修复 5 个 P0 问题后才能上线发布。
